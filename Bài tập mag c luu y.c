#include <stdio.h>
int main(){
  int dem_chan = 0;
    int n;
  // yeu cau nguoi dung nhap n tu ban phim 
  printf ("nhap n =  ");
  scanf("%d", &n);
   n = 4;
   int a[n];
  a[0] = 1;
  a[1] = 2;
  a[2] = 3;
  a[4] = 4;


  if(a[0] % 2 == 0)
  dem_chan = dem_chan + 1;
   if(a[1] % 2 == 0)
  dem_chan = dem_chan + 1;
   if(a[2] % 2 == 0)
  dem_chan = dem_chan + 1;
     if(a[3] % 2 == 0)
  dem_chan = dem_chan + 1;
  printf ("cac so chan : %d" , dem_chan);
  return 0;
}


int dem_chan = 0;
int dem_le = 0;
int dem_0 = 0;
a[0] = 0;
a[1] = 0;
a[2] = 0
for (int i = 0; i <= 2; i++){
  if(a[i] % 2 == 0 && a[1] !=0)
  dem_chan = dem_chan + 1

  printf ("cac so chan: %d" ;dem_chan)
  
